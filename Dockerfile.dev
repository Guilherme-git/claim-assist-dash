# Imagem para desenvolvimento com hot reload
FROM node:20-alpine

WORKDIR /app

# Copiar apenas dependências (código será montado via volume)
COPY package.json package-lock.json* ./

RUN npm ci

EXPOSE 8080

CMD ["npm", "run", "dev"]
